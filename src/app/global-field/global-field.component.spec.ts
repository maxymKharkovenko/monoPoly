import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GlobalFieldComponent } from './global-field.component';

describe('GlobalFieldComponent', () => {
  /*let component: GlobalFieldComponent;
  let fixture: ComponentFixture<GlobalFieldComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ GlobalFieldComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(GlobalFieldComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
